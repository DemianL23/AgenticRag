# 在同一仓库中分离数据准备、问答与接口

前端和一个 Python 后端包放在同一仓库中。后端将数据准备（ingestion）、在线问答（rag）和 HTTP 接口（api）分开：网页请求与批量评测复用同一问答入口，避免为了不同入口维护两套行为逐渐分歧的 RAG 流程。为方便个人开发与逐步学习，各职责先作为包内模块实现，不拆成独立应用或微服务。

V1 在 V0 上持续改进，不要求多个版本同时在线。历史基线通过代码版本、锁定依赖、配置和数据及评测产物保留；后续替换解析器或检索方式时，尽量复用网页接口和评测入口。
