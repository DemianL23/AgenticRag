# 使用 Milvus 作为向量数据库

项目采用 Milvus 保存文档片段向量及其来源元数据，执行相似度检索，并通过 Docker 管理运行环境。这是用户明确指定的技术选型；此前讨论过 Qdrant 和 PostgreSQL 加 pgvector，但没有以这些替代方案实现系统，也没有进行性能比较。

Milvus 的集合结构、索引配置及接入依赖会影响后续实现，替换数据库需要迁移或重建索引。应用仍应保留稳定的文档标识和来源信息，以支持引用核对与实验复现。具体 Milvus 版本和部署配置在环境准备阶段确定。
