# 财经文档问答实验

本项目围绕财经参考文档回答问题，逐步比较不同 RAG 版本在固定评测数据上的效果与代价。

## Language

**参考文档（Reference Document）**：供回答问题查找依据的文档。一份文档可以对应多道问答样本，其不同文件表示不自动构成不同文档。
_Avoid_：用“QA 文件”指代参考文档

**语料集（Corpus）**：某次实验允许检索的参考文档集合，不包含问答样本的参考答案和解题记录。
_Avoid_：用“数据集”同时指代语料集和评测集

**问答样本（QA Example）**：包含一个问题、参考答案及对应参考文档关系的评测单元。参考答案用于评价系统回答。
_Avoid_：用“一个文件”作为题目数量单位

**文档标识（Document ID）**：在当前实验数据中唯一标识一份参考文档的编号。文件重命名后仍可通过该标识关联题目和来源。
_Avoid_：以可能重复的文件名作为唯一身份

**RAG 版本（RAG Version）**：一种用于回答文档问题的处理方案，如 V0 基础检索问答和后续加入检索改进或多轮决策的方案。
_Avoid_：将 RAG 版本与模型服务商或模型名称混用

**检索基线（Retrieval Baseline）**：在固定语料集和检索评测集上，对一种检索策略单独记录的可复现实验结果；它不包含回答生成，也不等同于完整 RAG 版本的效果。
_Avoid_：仅完成检索评测便称为完整 RAG 基线

**评测集（Evaluation Set）**：用于评价 RAG 版本表现的一组问答样本。比较不同版本时，使用相同评测集，并明确所使用的语料集。
_Avoid_：用“知识库”指代评测集

**生成上下文（Generation Context）**：本次问答中，经检索并最终实际提供给回答模型的参考文档内容。检索后被排除的内容不属于生成上下文。
_Avoid_：把所有检索候选都称为本次生成上下文

**引用来源（Citation Source）**：本次回答所标注的、可追溯到生成上下文的参考文档或内容片段。来源身份真实与内容是否足以支持回答是两个需要分别检查的问题。
_Avoid_：模型凭空生成的文件名；将提供给模型等同于证明回答正确

**词法检索（Lexical Retrieval）**：按问题与文档片段之间的词项匹配及词频统计对候选片段排序的检索方式；本项目以 BM25 作为第一种词法检索方法。
_Avoid_：将其称为语义检索；用“关键词完全匹配”概括 BM25

**混合检索（Hybrid Retrieval）**：将语义检索与词法检索各自产生的候选排名合并为统一候选排名的检索方式。
_Avoid_：将两路原始分数直接相加也笼统称为混合检索结果

**倒数排名融合（Reciprocal Rank Fusion, RRF）**：仅依据候选在各路检索中的名次计算融合排名的方法，不比较不同检索器的原始分数尺度。
本项目默认对 Dense 和 BM25 各取 Top-20，使用 `rrf_k=60`，按 `chunk_id` 去重并保留两路 rank。
_Avoid_：原始分数归一化；加权分数融合

**候选池（Candidate Pool）**：Dense Top-20 与 BM25 Top-20 按 `chunk_id` 去重后形成的全部待排序片段，最多包含 40 个片段。候选池描述召回范围，不等同于最终返回给下游的 Top-K。
_Avoid_：只把 RRF Top-20 称为完整候选池；把最终结果称为候选池

**重排序（Reranking）**：在不新增候选的前提下，根据问题与候选片段的相关性重新排列候选池。重排序改变名次，不负责召回。
_Avoid_：重新召回；把 RRF 分数与重排序分数相加

**候选覆盖（Candidate Coverage）**：衡量相关片段是否已经进入某一检索阶段的候选范围。本项目分别记录 RRF Top-20 的覆盖和完整候选池的覆盖，二者不作为最终 Top-K 排序质量的替代指标。
_Avoid_：把完整候选池覆盖率写成最终结果的 Recall@20；用覆盖率证明重排序有效

**最终检索结果（Final Retrieval Result）**：候选池经过重排序后交给下游的有序 Top-K 片段；V1.2 默认取 Top-5。正常路径由重排序分数决定顺序，重排序失败时可退回 RRF 顺序。
_Avoid_：把最终结果称为新的召回；把 fallback 结果伪装成成功的重排序结果

**会话存储（Conversation Storage）**：持久保存会话及其中的用户问题、系统回答等消息，供日后查看。保存消息不意味着后续回答自动参考这些消息。
_Avoid_：将会话存储等同于会话记忆

**会话记忆（Conversation Memory）**：在处理当前问题时利用同一会话的历史内容，使回答能够承接前文。第一版不启用此能力。
_Avoid_：仅因保存了历史消息就称系统具有记忆

**检索任务（Retrieval Task）**：由一个用户问题直接形成或拆分得到的最小必要问题单元，能够独立检索和判断是否可回答。一个复杂问题可以包含多个检索任务，但任务之间不依赖彼此的答案。
_Avoid_：子 Agent；把任意关键词查询都称为检索任务

**查询修订（Query Revision）**：用户补充或选择范围后，对检索任务语义形成的一次明确更新。系统为改善检索表达而进行的自动改写不构成查询修订。
_Avoid_：Query Rewrite；把每次查询文本变化都称为修订

**检索尝试（Retrieval Attempt）**：在同一个查询修订内，使用某一种明确检索表达和策略执行的一次完整检索。检索尝试描述执行历史，不代表产生了新问题语义。
_Avoid_：Query Revision；隐藏式重试

**证据（Evidence）**：来自参考文档、可由稳定 chunk 标识追溯的内容片段，是中间结论和最终回答能够引用的唯一事实来源。
_Avoid_：将 Query Rewrite、Step-back、HyDE 或模型生成文本称为证据

**证据评估（Evidence Grade）**：对某个检索任务当前证据的相关性、可回答性、歧义和可恢复性所作的结构化判断。证据评估描述证据状态，不直接决定工作流动作。
_Avoid_：Route；用单一模糊分数代替不同判断维度

**恢复检索（Recovery Retrieval）**：首次证据不足但仍可能改善时，在受限预算内改变检索表达并再次检索的动作。它不改变用户问题语义，也不形成新的查询修订。
_Avoid_：无限重试；HITL 用户澄清

**有据中间结论（Grounded Finding）**：针对一个检索任务、由已验证证据支持并保留证据标识的中间回答。它是供最终合成使用的结论，不是新的证据。
_Avoid_：Evidence；无来源的子答案

**执行状态（Execution Status）**：描述请求或检索任务当前是否运行、等待、完成或因技术问题失败。执行完成只表示流程终止，不代表问题已经得到完整回答。
_Avoid_：Answer Outcome；把知识不足称为执行失败

**回答结果（Answer Outcome）**：描述工作流在业务上是否完整回答、部分回答、无知识、不支持或仍未解决。它与模型、检索或持久化是否正常执行分别记录。
_Avoid_：Execution Status；从最终自然语言中反推结果类型

**无知识（No Knowledge）**：系统正常完成检索和判断，但当前知识库证据不足以支持回答。它不表示任务超出系统能力，也不表示执行发生故障。
_Avoid_：Unsupported；Technical Failure

**不支持（Unsupported）**：问题需要当前 RAG 版本明确未提供的能力，因此系统有意停止该任务。它不表示知识库没有相关资料，也不表示系统发生技术故障。
_Avoid_：No Knowledge；Technical Failure

**未解决（Unresolved）**：系统确认仍需用户消除歧义，但允许的人机确认预算已经耗尽，因而无法继续完成任务。
_Avoid_：No Knowledge；Unsupported

**技术失败（Technical Failure）**：模型、检索、校验或持久化等执行环节未能按契约完成，导致任务或请求无法正常推进。它不构成回答结果，不能被改写为无知识或不支持。
_Avoid_：No Knowledge；Unsupported；Unresolved

**工作流检查点（Workflow Checkpoint）**：为暂停后继续同一请求而保存的可恢复工作流状态。它用于执行连续性，不等同于会话存储或会话记忆。
_Avoid_：Conversation Storage；Conversation Memory

**降级检索（Degraded Retrieval）**：检索流程通过已定义的备用路径返回结果，但没有完成首选排序路径。降级结果仍可使用，但必须与正常检索明确区分并单独统计。
_Avoid_：把 fallback 伪装成正常 Reranker 输出
